{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/${fileBasenameNoExtension}.exe",
            "args": [
                "Hello",
                "world!"
            ],
            "cwd": "${workspaceRoot}"
        }
    ]
}